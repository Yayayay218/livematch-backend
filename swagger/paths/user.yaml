get:
  tags:
    - users
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/UserInformation'
put:
  tags:
    - users
  parameters:
    - name: data
      description: user object
      required: true
      in: body
      schema:
        $ref: '#/definitions/UserInformation'